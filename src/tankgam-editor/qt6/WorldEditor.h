#pragma once

#include <QMainWindow>

class WorldEditor : public QMainWindow
{
    Q_OBJECT
    
public:
    explicit WorldEditor(QWidget* parent = nullptr);
    ~WorldEditor() override;
    
private:

};
